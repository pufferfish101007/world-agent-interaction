# gemini api key, obtained from https://aistudio.google.com
GEMINI_API_KEY = ""

# model to use. gemini 3.1 flash lite by default because, at the time of writing,
# it has the most generous ratelimits on the free plan while also supporting
# the features we want (structured output and function calls).
MODEL = "gemini-3.1-flash-lite"